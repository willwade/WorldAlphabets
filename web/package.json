{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm-run-all predev vite-dev",
    "vite-dev": "vite",
    "predev": "cpr ../data ./public/data -o && cpr ../audio ./public/audio -o",
    "build": "npm run predev && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^5.3.1",
    "cpr": "^3.0.1",
    "npm-run-all": "^4.1.5"
  }
}
